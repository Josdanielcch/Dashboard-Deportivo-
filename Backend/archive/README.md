# Archivo de scripts

Scripts de migración one-off ya ejecutados contra la base de datos y scripts rotos/desechables.
Se conservan solo como referencia histórica.

**Importante:** Estos scripts NO se ejecutan desde esta ubicación. Sus rutas relativas
(`../src/config/database`) apuntaban a su ubicación original (raíz o `scripts/`).

Para reconstruir un entorno desde cero usar el respaldo completo en
`Database/ULTIMO RESPALDO DB.sql`, no estos scripts.
